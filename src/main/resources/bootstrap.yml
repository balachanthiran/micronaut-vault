micronaut:
  application:
    name: vault-test
  config-client:
    enabled: true

vault:
  client:
    config:
      enabled: true
    uri: http://127.0.0.1:8200
    token: myroot
    kv-version: V1
    secret-engine-name: secret
    fail-fast: false
